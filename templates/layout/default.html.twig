<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>ElePHPant Webshop</title>

        <link rel="stylesheet" href="{{ asset('static/css/bootstrap.min.css') }}" />
        <link rel="stylesheet" href="{{ asset('static/css/bootstrap-theme.min.css') }}" />
        <link rel="stylesheet" href="{{ asset('static/css/style.css') }}" />
    </head>
    <body>
        <div class="container">
            <nav class="navbar navbar-default">
                <div class="container-fluid">
                    <div class="navbar-header">
                        <a class="navbar-brand" href="/">
                            <img src="{{ asset('static/images/logo.gif') }}" class="img-responsive" width="50" />
                        </a>
                    </div>

                    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                        <ul class="nav navbar-nav">
                            <li><a href="/">My Order</a></li>
                            <li><a href="{{ path('checkout/step', {'step': 'details'}) }}">Checkout</a></li>
                        </ul>
                    </div>
                </div>
            </nav>
        </div>

        <div class="container">
            {% block content %}{% endblock %}
        </div>
    </body>
</html>